# keys.env — OPTIONAL API keys for the Shate pipeline.
#
# ⚠️  THIS FILE IS COMMITTED TO A PUBLIC REPOSITORY ON PURPOSE.
# The repo owner explicitly accepted the leak risk for these keys
# (2026-09-06: "dej tam klidně i api klíč… me je to fakt jedno jestli
# se to leakne"). Only ever add keys the owner has cleared for this.
# Anything sensitive belongs in the local, git-ignored `.env` instead
# (cp .env.example .env) — never commit `.env`.
#
# Lookup order for agents: `.env` first, then this file. `.env` wins.
#
# Note: deleting a key here does NOT remove it from git history — if a
# key must be revoked, rotate it at the provider, then clean history.

# --- Step 03: Voiceover — ElevenLabs upgrade (Path B) ---
ELEVENLABS_API_KEY=sk_5233ce5bc64531056715296b346226a1992a0775b01996af

# --- Future keys below (owner-approved only) ---
# OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
